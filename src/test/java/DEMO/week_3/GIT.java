package DEMO.week_3;

public class GIT {

    // GIT - to move codes from local (comp) to remote (github) repository

    // git --version

    // git init - locally initialize (create) empty Git repository - red classes are untracked
    // git config --add user.name kanybek
    // git config --get user.name
    // git status - shows only untracked files
    // git add * - make ready files / allow all to track
    // git commit -m firstcommit (firstcommit is just a text -can write any) - send to remote repository
    // git branch - shows branch: * master (common branch) or + name (create branch)
    // git switch kan - move to kan branch
    // git merge master - merge / import files from master to kan
    // git remote add origin https://github.com/kanybek1989/gitPractice.git - merge remote with local repository
// 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
    // git pull origin master - merge from remote to local (master)

}
